#import "../preamble.typ": *

= Hypotheses

  == Travel Times
    
      
  == Planning data
  // TODO needs a better name
      
<end_of_chapter>

#locate(loc => bib_state.at(query(<end_of_chapter>, loc).first().location()))